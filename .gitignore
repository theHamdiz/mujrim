# Build artifacts
/target
/target-*
**/target
rust_out

# IDE / editor
.idea/
.vscode/
.venv/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
*Icon
Icon*
Icon
!**/icons.rs

# Debug
*.pdb
.cargo/*
.cargo

# PGO data
/tmp/mujrim-pgo/

# Syzygy tablebases (downloaded at runtime)
syzygy/

# NNUE networks (downloaded at runtime)
/nnue/

# External NNUE networks in resources (only the embedded net.bin is tracked)
crates/mujrim-eval/resources/*.nnue
crates/mujrim-eval/resources/*.bin
crates/mujrim-eval/resources/*.net
crates/mujrim-eval/resources/*.nnue.zst
crates/mujrim-eval/resources/akimbo-*.bin
crates/mujrim-eval/resources/stockfish-*.nnue
crates/mujrim-eval/resources/viridithas-*
!crates/mujrim-eval/resources/ak_default.bin
!crates/mujrim-eval/resources/reckless_v60.nnue

crates/mujrim-ui/target/*
crates/mujrim-game/target/*
crates/mujrim-installer/target/*

# SPRT test results
sprt/results.pgn

# Dist trees (built locally; do not commit binaries)
/dist/

# Local scratch / downloaded third-party engine trees
.tmp*/
.tmp-*
.tmp*

# Vendored engines (download via scripts/vendor-linux-engines.sh)
/engines/

# Local Windows cross SDK cache (cargo-xwin / packaging helpers)
.xwin-cache/
