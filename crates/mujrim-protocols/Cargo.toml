[package]
name = "mujrim-protocols"
version = "1.0.0"
edition = "2024"
authors = ["Ahmad Hamdi Emara"]
description = "Trait-based UCI/XBoard protocol clients for external chess engines"
license = "MIT"

[lib]
name = "mujrim_protocols"
path = "src/lib.rs"

[dependencies]
