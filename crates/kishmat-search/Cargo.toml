[package]
name = "kishmat-search"
version = "2.0.0"
edition = "2024"
authors = ["Ahmad Hamdi Emara"]
description = "Search engine for the Kishmat chess engine"
license = "MIT"

[dependencies]
types = { package = "kishmat-types", path = "../kishmat-types" }
eval = { package = "kishmat-eval", path = "../kishmat-eval" }